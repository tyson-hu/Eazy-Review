---
description: Applies to UI, layout, and component work
globs:
  - "app/**/*.tsx"
  - "components/**/*.tsx"
  - "src/components/**/*.tsx"
  - "src/features/**/*.tsx"
---

For UI work, read the affected sections of `docs/DESIGN.md` and
`docs/USER_FLOWS.md`. DESIGN owns tokens, components, score presentation,
accessibility and screen rules; research documents are non-authoritative.
Reuse existing `src/components/ui/` primitives. Ordinary UI implementation needs
no skill; runtime journey or screenshot evidence uses interactive-preview-loop.

/**
 * Auth hooks re-export. Prefer importing from this module or AuthProvider.
 */
export { AuthProvider, useAuth } from '@/src/features/auth/AuthProvider';
export type {
  AuthContextValue,
  AuthProviderProps,
} from '@/src/features/auth/AuthProvider';

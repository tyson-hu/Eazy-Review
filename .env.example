# Expo-safe client placeholders only. Copy to `.env` for local work.
# Values below are obviously fake — never commit real credentials.
# Do NOT add service-role keys, DB passwords, JWT secrets, or management tokens.
# See docs/SECURITY.md (Expo may contain only project URL + publishable/legacy anon key).

EXPO_PUBLIC_SUPABASE_URL=https://example-local.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eazy-review-fake-anon-key-not-real
